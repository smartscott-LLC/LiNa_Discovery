[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "ipc_bridge"
version = "0.1.0"
requires-python = ">=3.10"
license = { text = "MIT" }

[tool.maturin]
python-source = "python"
module-name = "ipc_bridge._ipc_bridge"
