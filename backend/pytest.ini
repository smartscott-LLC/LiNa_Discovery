[pytest]
# Scope pytest to LINA's own tests — the repo-root pyproject.toml belongs
# to passagemath and uses a different filename pattern (*_test.py).
python_files = test_*.py
python_classes = Test*
python_functions = test_*
testpaths = lina ipc triton
addopts = -p no:warnings
